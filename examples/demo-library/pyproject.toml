[project]
name = "tinyretry"
version = "0.2.0"
description = "A small retry-with-backoff decorator. Fixture for the launch-compliance scanner."
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
